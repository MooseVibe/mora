# Features · Mora

## Готово (MVP)

### Авторизация
- Google OAuth
- Обычная авторизация (email/пароль)
- Сессия сохраняется

### Карта дня (неавторизованный)
- Можно вытянуть карту без регистрации
- Карта показывается с описанием
- Кнопка «Сохранить карту» предлагает залогиниться
- Кнопка «Поделиться»

### Карта дня (авторизованный)
- Карта вытягивается раз в день
- Защита от повторного вытягивания — нельзя вытянуть второй раз в тот же день
- Карта автоматически сохраняется в дневник
- В колоде 31 карта: 22 старших аркана и 9 пилотных младших/придворных карт
- Уже вытянутую карту дня можно развернуть в full-result экран и вернуть назад shared-element анимацией

### Дневник карт (в личном кабинете)
- История всех вытянутых карт по дням
- Просмотр карты + описания, которое было в день вытягивания
- Навигация по датам

### Сводка по 3 последним картам (на дашборде)
- Показываются 3 карты за последние 3 дня
- Подпись «summary» под ними — в MVP **захардкожена**
- В будущем — генерация AI

## В работе (UI-редизайн)

| Экран | Статус |
|---|---|
| Главный (лендинг) | ✅ Готов, остались мелкие баги |
| Авторизация | ✅ Готов, остались мелкие баги |
| Дашборд | 🟡 Сейчас в работе |
| Дневник карт | ⬜ Не начат |
| Просмотр одной карты | ⬜ Не начат |
| Профиль / настройки | ⬜ Не начат |

## Следующий шаг

### Dashboard recent cards → full-result reader

Следующая задача: сделать карты из блока recent cards / «Дневник карт» на dashboard интерактивными.

Ожидаемое поведение:
- пользователь нажимает на одну из последних карт;
- dashboard UI уходит в `opacity: 0`, как в текущем draw/result flow;
- выбранная карта летит из своей плитки в full-result позицию справа;
- слева появляется название, дата вытягивания и текст именно этой записи;
- кнопка «Назад» возвращает карту обратно в ту же плитку shared-element анимацией.

Технические ориентиры:
- переиспользовать подход `DashboardCardReader`, а не создавать отдельный overlay с нуля;
- сначала сделать один визуальный инкремент для recent-card плитки и получить визуальный аппрув;
- отдельно проверить `card_draws.variant_idx`: для точного текста дневника запись должна хранить вариант текста, старые записи без `variant_idx` могут использовать fallback;
- не добавлять новые тяжёлые ассеты и не коммитить временные PNG-source;
- держать производительность как сейчас на production: WebP-first, без лишних фоновых слоёв и без перерендера всего dashboard во время анимации.

## Планируется (после редизайна)

### Большое — AI-таролог в чате
Главная фича проекта. Пользователь задаёт вопрос → AI делает расклад → объясняет карты → ведёт диалог. Прикручивается после редизайна.

### Среднее
- Замена захардкоженной «сводки 3 карт» на реальную AI-генерацию
- Уведомления о новой карте дня (push / email)
- Платная подписка (детали не проработаны)

### Маленькое
- Анимация переворота карты
- Шаринг карты в соцсети с красивой картинкой
- Тёмная/светлая тема (если решим что нужно — пока тёмная одна)

## Не делаем (явно)

- Метрики и аналитика — рано, сначала продукт.
- Команда, шаринг карт между пользователями — это не социалка.
- Расклады на нескольких картах вручную — это ломает фокус на AI-таролога.
- Покупка отдельных карт, NFT, gamification — это не суть продукта.
