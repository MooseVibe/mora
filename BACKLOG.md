# Mora — Release Backlog

Этот файл — место для багов, UX-долгов и наблюдений с MVP-тестов, которые нельзя потерять до публичного релиза.

> Legacy dashboard/journal/auth backlog ниже сохранён только как историческая запись. Эти маршруты архивированы и удалены из production tree 2026-08-23; возвращать их без отдельного продуктового решения не нужно. Актуальный release-план находится в `docs/features.md` и `docs/current-work.md`.

Это не `docs/decisions.md`: сюда попадает то, что нужно проверить, исправить или переосмыслить. Когда задача сделана и подтверждена визуально/продуктово, её можно перенести в changelog/decision или удалить из активного списка.

## Легенда

- **Critical** — мешает нормальному использованию или ломает ключевой сценарий.
- **High** — сильно портит опыт, но есть обходной путь.
- **Medium** — заметный UX/UI-долг.
- **Low** — полировка.

Статусы:
- `Open` — записано, не взято в работу.
- `In progress` — сейчас чинится.
- `Needs QA` — нужен ручной/визуальный тест.
- `Done` — исправлено и подтверждено.

## Product Roadmap

### Ближайшая последовательность

1. **Гипотеза отклика на карту.** Последняя заметная продуктовая механика перед макетами: сделать маленький MLP “первого отклика”, а не проверки предсказания. На dashboard уже есть локальный one-shot `Принимаю / Не принимаю` для сегодняшней карты и раскрытых recent-карт. Следующий отдельный шаг — решить сохранение в Supabase/journal и финальную формулировку вариантов. Не делать coins, статистику, вечерние проверки или AI-объяснения в первом проходе.
2. **Cards-first режим.** Полная колода из 78 карт и визуальная унификация завершены после отдельного финального прохода `three-of-swords`. Следующие карточные задачи ограничены восемью старыми картами без утверждённых `preview/full/share`; обновлять их по одной, не меняя ID или механику draw/save/journal.
3. **Journal visual polish.** После уже подключённого reader-паттерна улучшить визуал списка, пустого состояния, фильтров `всё время / 30 дней / 7 дней`, action-зоны и mobile/desktop layout. Текущие кнопки `Сбылось / Не сбылось / Мои заметки` в journal есть визуально, но пока не сохраняют состояние.
4. **Responsive dashboard + journal.** Довести dashboard и journal до нормального состояния на 375px, 1440px и промежуточных ширинах, не ломая существующие shared-element анимации.
5. **Dashboard polish.** Улучшить header, профиль/настройки, блок `Походы к Тарологу` как красивую честную заглушку, блок `Совет дня` и его поведение при появлении будущего баннера отклика.
6. **Unauthenticated flow / landing.** Вернуться к неавторизованному сценарию: fresh result, share, мягкий переход к регистрации, сохранение pending draw, возможно полный редизайн лендинга после авторизованного ядра.

### Правило ведения плана

После каждого завершённого шага из roadmap обновлять этот раздел: переносить сделанное в `Done` или в список уже реализованного, уточнять следующий шаг и удалять устаревшие формулировки. Если меняется продуктовая логика или порядок работ, дополнительно фиксировать решение в `docs/decisions.md`. Нельзя оставлять в плане будущей задачей то, что уже реализовано в коде.

### Уже реализовано в текущей истории

- Dashboard recent cards уже кликабельные: `RecentCardsWidget` открывает каждую предыдущую карту через `DashboardCardReader`, использует `getDrawReading(draw)` и показывает сохранённый/ fallback-текст.
- Dashboard recent-card reader уже показывает локальный one-shot отклик `Принимаю / Не принимаю`, хранит его в `localStorage` по `drawn_at + card_id` и не выводит статус на самой плитке.
- Dashboard fresh result после `Вытянуть карту` использует native draw-flow только для ritual-анимации и сохранения, а после завершения открывает тот же `DashboardCardReader`, что и expand уже вытянутой карты.
- Уже вытянутая карта дня на dashboard открывается кликом по карте или Expand-кнопкой через тот же reader-паттерн.
- Share остаётся на fresh native/unauth result-screen и иконкой у свернутой сегодняшней карты на dashboard; в раскрытом dashboard reader отдельной share-кнопки нет.
- `/api/draws` сохраняет `variant_idx` и `reading_snapshot` для новых вытягиваний, с fallback на старую схему при отсутствии колонки `reading_snapshot`.
- `/journal` выбирает `card_id, drawn_at, variant_idx, reading_snapshot`, читает записи через `getDrawReading()`, раскрывает строки через `DashboardCardReader` и показывает сохранённый/fallback reading. Outcome/note пока не сохраняются.

### Принцип для дневника

Дневник карт должен стать не архивом ради архива, а историей ежедневных ритуалов: какая карта выпала, какой именно текст был показан, поделился ли пользователь, и позже — принял ли пользователь карту, не принял её или пока не понял. Это важно не только для текущего retention, но и как будущий персональный контекст для AI-таролога.

## Done

### Medium · Journal MLP 2: записи раскрываются в full-result reader

**Статус:** Done
**Дата:** 2026-06-19
**Где:** `/journal`, `src/app/journal/page.tsx`, `src/components/JournalClient.tsx`
**Источник:** сверка roadmap с кодом
**Подтверждение:** код уже выбирает `card_id, drawn_at, variant_idx, reading_snapshot`, использует `getDrawReading(draw)` и открывает каждую строку через `DashboardCardReader`.

**Что сделано:**
- `/journal` показывает desktop table layout с фильтрами периода;
- строки кликабельны и открываются в full-result reader;
- новые записи читаются через `reading_snapshot`, старые — через fallback;
- share в journal-reader доступен только для сегодняшней записи с утверждённым `shareText`.

**Что осталось:**
- visual polish дневника;
- mobile/responsive доводка;
- будущее сохранение outcome/note отдельной задачей.

### Low · Длинные названия карт в dashboard recent cards выглядят тяжело

**Статус:** Done
**Дата:** 2026-06-12
**Где:** `/dashboard`, блок «Дневник карт» / recent cards
**Источник:** визуальная ретроспектива после проверки card reader
**Подтверждение:** локально проверено кодом и сборочными проверками: названия в recent-card плитках ограничены одной строкой, длинные значения обрезаются через ellipsis, полное название доступно через hover title.

**Что сделали:**
- запретили перенос названия карты на вторую строку в `.rcw-name`;
- добавили `overflow: hidden`, `text-overflow: ellipsis`, `white-space: nowrap`;
- добавили `title` с полным названием карты.

**Почему важно:**
Блок «Дневник карт» должен выглядеть как ровный визуальный архив. Длинные названия не должны увеличивать отдельные плитки и создавать неровный ритм.

### Critical · После посещения дневника карта дня не вытягивается с главной

**Статус:** Done  
**Дата:** 2026-06-08  
**Где:** переход `/journal` -> главная / dashboard daily draw flow  
**Источник:** MVP-тесты
**Подтверждение:** локально проверено 2026-06-08 через `/dashboard?preview=empty` → `/journal?returnPreview=empty` → `/dashboard?preview=empty`: после возврата карта вытягивается.

**Сценарий воспроизведения:**
- зайти в дневник карт;
- вернуться на главную;
- попробовать вытянуть карту;
- ничего не происходит.

**Что проверить при фиксе:**
- не остаётся ли после `/journal` состояние/класс на `body`, overlay, loader или routing transition, которое блокирует draw;
- не ломается ли инициализация `draw.js` / `TaroApp` после возврата со страницы дневника;
- не перехватывает ли клики невидимый слой;
- не конфликтует ли authenticated/dashboard flow с unauthenticated landing draw flow;
- проверить сценарий через обычную навигацию, browser back и прямой переход по URL.

**Почему важно:**
Это ломает ключевой сценарий продукта — вытягивание карты. Баг нужно держать в списке до фикса, но сейчас не чинить в рамках текущей записи.

## Open

### Critical · Один тестер не может открыть moratarot.com в Safari

**Статус:** Needs QA evidence
**Дата:** 2026-08-29
**Где:** production `https://moratarot.com`, Safari, прямая ссылка и ручной ввод адреса
**Источник:** полевой тест автора

Один тестер не получает сайт ни с VPN, ни без него. На момент повторной диагностики authoritative/public DNS через Google и Cloudflare возвращает единственную A-запись `76.76.21.21` с TTL `300`, конфликтующей AAAA-записи нет, TLS-цепочка `moratarot.com` валидна, текущий origin отвечает `200`. Расширенный Check-Host подтвердил `200` из Москвы и всех проверенных узлов вне нескольких иранских таймаутов. Это исключает текущий глобальный outage, но не локальный DNS/ISP/Safari/content-blocker сбой.

Для root-cause нужен скрин точного экрана Safari с текстом ошибки, время попытки, версия iOS/macOS и тип сети. Без этого нельзя безопасно выбирать между очисткой DNS/browser state, ISP/CDN-маршрутом и исправлением уже загруженной страницы. DNS/CDN не менять вслепую.

### Critical · Mobile saved daily card появляется не единым компонентом

**Статус:** Needs QA
**Дата:** 2026-08-29
**Где:** Mora Next `/ritual`, сохранённая карта дня на mobile Safari
**Источник:** production-наблюдение автора

`daily-result-ready` открывал mobile DOM-card независимо от готовности `#daily-result-image`, а `populateDailyResult()` назначал `src` без `decode()`. Теперь весь `.daily-result-card` скрыт до декодирования актуального URL и открывается одним классом; повторное заполнение тем же уже готовым изображением не прячет компонент заново. Ошибка загрузки не оставляет результат навсегда скрытым. Fix commit `4e10ee6` выпущен в production deployment `dpl_D7PxVZoQ6ZYmt3XkywdwY8xcEQZg`; нужен production QA cold/warm входа на реальном iPhone.

### Medium · SVG-иконка масти/аркана появляется позже текста тега

**Статус:** Needs QA
**Дата:** 2026-08-28
**Где:** Mora Next `/ritual`, только что созданный расклад и переход в чтение
**Источник:** production-наблюдение автора

Иногда текст названия масти/аркана уже виден, а SVG-маркер дорисовывается последним, из-за чего готовый тег заметно меняется после появления. Второй production QA показал задержку даже после перехода на нативный `<img>` и при повторном входе. Фактическая общая причина найдена в response headers: Vercel отдавал `/ritual/icons/*.svg` как `max-age=0, must-revalidate`, а компонент повторно назначал `src`, поэтому браузер мог заново проверять SVG через сеть. В `instantdraw7` все ritual icons получают существующий static-cache contract `max-age=86400, stale-while-revalidate=604800`, а `populateCardTag()` не назначает уже установленный URL повторно. Три динамических SVG по-прежнему проходят ранний `decode()`. Нужна production-проверка первого и повторного переходов: текст и маркер должны быть видны в одном первом кадре.

### Medium · Artwork выглядит мутным на крупной 3D/GLB-карте дня

**Статус:** Done
**Дата:** 2026-08-27
**Где:** Mora Next `/ritual`, крупная лицевая карта дня в WebGL/GLB
**Источник:** визуальная проверка автора после новых генераций

Canonical WebP выглядел заметно резче в DOM-контекстах, чем тот же artwork на крупной 3D-карте дня. Причина подтверждена сравнительным локальным QA: desktop daily renderer ограничивал Retina DPR значением `1.5`, тогда как DOM использовал DPR `2`. Daily-only cap поднят до `2`; spread renderer остаётся на `1.5`, GLB, artwork, материал и свет не менялись. Автор подтвердил, что крупная 3D-карта стала намного резче.

### High · Открытая вкладка карты дня показывает смешанный экран после истечения cooldown

**Статус:** Done
**Дата:** 2026-08-22
**Где:** Mora Next, desktop «Карта дня», вкладка с уже вытянутой картой
**Источник:** авторский тест и скриншот сломанного состояния

**Сценарий воспроизведения:**
- вытянуть карту дня и оставить вкладку с результатом открытой;
- дождаться истечения cooldown, в том числе пока вкладка находится в фоне;
- вернуться во вкладку;
- заголовок и подзаголовок уже переходят в idle-состояние, но старая result-карта остаётся справа, поэтому на экране одновременно видны части двух состояний.

**Ожидаемое поведение:**
- по истечении cooldown весь daily-flow атомарно возвращается в чистый idle: заголовок, DOM-состояние, классы, WebGL-controller и старая result-карта сбрасываются вместе;
- при `visibilitychange` и возврате фокуса срок повторно сверяется с каноническим временем, потому что фоновые вкладки могут замораживать или задерживать таймеры;
- после возврата пользователь видит обычную интерактивную колоду и может вытянуть новую карту без перезагрузки страницы.

**Что проверить при фиксе:**
- существующий контракт `resetResultToIdle()` действительно очищает все DOM/WebGL/result-состояния, а не только часть интерфейса;
- истечение срока при активной вкладке и при вкладке в фоне;
- desktop и mobile, guest и authenticated состояния, не меняя Supabase/auth-механику;
- не добавлять запасную CTA-кнопку как постоянный обход: корректный полный reset является основным решением.

### High · Первый клик по готовой 3D-колоде карты дня мог потеряться

**Статус:** Done
**Дата:** 2026-08-12
**Где:** Mora Next, первый экран после подтверждённого входа

Видимая 3D-колода могла оставаться нативно `disabled` до завершения account hydration; кроме того, click доверял hover/raycast только от предыдущего `pointermove`. Подготовленный фикс запускает единственный account-state запрос параллельно с 3D, принимает первый клик по уже видимой колоде и автоматически продолжает его после безопасного подтверждения state. Raycast пересчитывается по координатам самого click. Loader и motion-flow не меняются.

### High · Навигация чтения могла остаться поверх карты дня

**Статус:** Done
**Дата:** 2026-08-12
**Где:** Mora Next, завершение расклада → переключение на карту дня

Отложенный `showReading()` после Gemini и callbacks перехода saved ↔ reading могли дописать spread-классы уже после переключения в `daily-mode`. Подготовленный фикс очищает все reading transition-классы при входе в карту дня и запрещает отложенным callbacks менять DOM вне spread-mode. Motion-flow и тайминги не меняются.

### Critical · Mora Next показывала localStorage другого пользователя после смены email

**Статус:** Production · Needs cross-device author QA
**Дата:** 2026-08-12
**Где:** Mora Next, login/reload/card day/saved spread

Общие ключи `dailyCard`, `pendingDailyCard` и `lastSpread` принадлежали origin браузера, а не аккаунту; обычный email также не подтверждался. Production-пакет вводит OTP для всех email и закрытый `prototype_account_states` по Supabase Auth `user_id`. Для аккаунта server-state становится источником истины, legacy localStorage очищается, login/logout сбрасывает DOM/WebGL через reload. Серверные smoke и permission-check прошли; осталось проверить один account на двух устройствах и два account в одном браузере, затем перевести задачу в Done.

### High · 3D-колода расклада могла зависнуть перед полётом карты

**Статус:** Production · Needs author cold-cache smoke
**Дата:** 2026-08-10
**Где:** Mora Next, выбор трёх карт
**Источник:** повторяющийся production-сигнал с разных устройств

**Что исправлено и оптимизировано:**
- GLB-клоны создаются лениво только для видимой части колоды, невидимые карты и тени исключены из draw calls;
- fullscreen starfield полностью удалён из production вместе с постоянным render-loop;
- постоянный render-loop карты дня приостанавливается в табе раскладов;
- DPR 3D-карт ограничен `1.5` на desktop и `1.2` на mobile;
- рубашка переведена из 3.3 MB PNG в визуально проверенный 718 KB WebP quality 95;
- resize камеры и renderer выполняется только при реальном изменении viewport;
- сохранены геометрия, click/drag-flow и тайминги анимаций.
- отменено глобальное ожидание daily WebGL; отклонённая автором искусственная CSS-стопка удалена, а точный 3D-canvas появляется только после готовности;
- исправлен click/drag race, из-за которого обычный click мог быть отброшен;
- новый расклад заранее выбирает через Web Crypto ровно три уникальные карты, сохраняет draft в `sessionStorage` и прогревает именно эти face-текстуры с приоритетом первой;
- прямой authenticated cold-entry теперь поднимает WebGL-controller до показа spread-screen, поэтому видимый CSS-fallback не заменяет 3D-веер после восстановления сессии;
- face fetch/decode/GPU-upload полностью удалены из критического пути: карта начинает утверждённую траекторию сразу, а редкий cache miss показывает имя и shimmer до готовности той же текстуры;
- production smoothness-pass ограничивает spread WebGL до DPR `1.5`, сокращает невидимый overscan и переносит урезанный preload четырёх лиц за пределы discovery-motion/инерции; остаточный риск проверяется на реальном iPhone/слабом laptop;
- daily idle-loop остановлен, а realtime shadow-pass ограничен верхней картой стопки и отключён при скрытом receiver веера;
- «Ещё расклад» сбрасывает готовую сцену in-page без повторной загрузки session, Three.js и GLB.
- автор подтвердил локальный полный выбор трёх карт без лагов на desktop и реальном телефоне по Wi-Fi; после deploy остаётся cold-cache smoke на `moratarot.com`.

**Подтверждение:** локально прошли daily ritual/result и повторный вход, полный desktop click-flow из трёх карт, возврат, in-page «Ещё расклад», повторный выбор после reset, mobile first-paint/crossfade и отсутствие console errors. Для click-latency пакета отдельно прошли `git diff --check`, JS syntax, lint и production build. Commits `7bfee5e` и `00f1e82` выпущены как production deployment `dpl_HgkvZ7GapBDvAeFfCSHWggiAfAtd`; smoke подтвердил новые модули и защиту AI-маршрута. Следующий шаг — повторный визуальный тест на реальных устройствах. GPU instancing или предвыбор трёх карт делать только если этот retest покажет остаточную просадку.

### Critical · Шаг выбора карт не объясняет основное действие

**Статус:** Open
**Дата:** 2026-07-27
**Где:** Mora Next prototype, шаг 2 трёхкарточного расклада
**Источник:** модерируемые тесты, ключевые сигналы повторились у 3 из 4 респондентов

**Что видно:**
- большинство не понимает, куда нажать после выбора темы;
- выбранный чип выглядит главным действием и уводит пользователя назад;
- пустые плейсхолдеры воспринимаются как кнопки автоматического заполнения;
- связь между нижней колодой и тремя позициями не считывается;
- drag-and-drop как требуемое действие пользователи самостоятельно не обнаруживают.

**Почему важно:**
Это блокирует главный сценарий нового продукта до появления результата расклада. Визуально эффектная колода не компенсирует отсутствие ясного следующего действия.

**Ориентиры для отдельной UX-итерации:**
- не менять одновременно экран результата, AI-тексты и анимации;
- сделать state выбора карт самодостаточным: одна ясная команда и один главный интерактивный объект;
- понизить выбранную тему из CTA до спокойного контекста; возврат к темам не должен конкурировать с выбором карты;
- объяснить связь «колода → следующая свободная позиция» через визуал и первый feedback;
- сохранить click/tap по карте как основной доступный путь, drag-and-drop — как приятный альтернативный жест;
- после изменения повторить короткий тест именно на переходе «выбрал тему → выбрал первую карту».

### High · Возврат в подробное чтение скрыт за swipe/scroll-жестом

**Статус:** Open
**Дата:** 2026-07-31
**Где:** Mora Next prototype, главный экран завершённого трёхкарточного расклада
**Источник:** модерируемые тесты, 3 из 3 респондентов, не привыкших к тачпаду

**Что видно:**
- по заданию вернуться в деталку все респонденты сначала нажимали на карты;
- затем проверяли чип выбранной категории и логотип Mora;
- никто не обнаружил переход в подробное чтение свайпом;
- экран не показывает явного кликабельного действия для возврата к чтению.

**Почему важно:**
Скрытый жест делает сохранённый результат похожим на тупиковый экран. Пользователь уже получил расклад, но не может повторно открыть его основную ценность — связное подробное чтение.

**Ориентиры для отдельной UX-итерации:**
- не полагаться на swipe/scroll как на единственный способ навигации;
- добавить видимую кликабельную точку входа в подробное чтение для mouse, keyboard и touch;
- сохранить жест как дополнительный быстрый путь;
- отдельно решить, должны ли точкой входа стать карты, самостоятельный CTA или оба варианта;
- повторить то же задание после изменения и проверить, находится ли действие без подсказки.

### Medium · Проверить, обнаруживается ли прокрутка колоды после анимированной подсказки

**Статус:** Needs QA
**Дата:** 2026-07-31
**Где:** Mora Next prototype, выбор трёх карт после выбора темы
**Источник:** пользовательские тесты: респонденты не понимали, что веер можно прокручивать

**Что сделано:**
- после выбора темы колода один раз плавно прокатывается примерно на пять карт;
- ручной wheel/drag прерывает движение;
- при `prefers-reduced-motion` автоматический прокат отключён;
- перед новым раскладом позиции колоды автоматически перемешиваются без совпадений с предыдущим порядком.

**Что проверить:**
- понимают ли новые респонденты без подсказки, что веер можно прокручивать;
- не воспринимается ли движение как декоративная анимация;
- не мешает ли оно сразу выбрать карту;
- одинаково ли считывается подсказка с мышью, тачпадом и на touch-экране.

### High · Перегенерировать визуал Шута

**Статус:** Open
**Дата:** 2026-06-19
**Где:** `public/assets/cards/fool.webp`, карта `fool` / «Шут»
**Источник:** MVP-фидбек из чата + скрин автора

**Что видно:**
- текущий Шут слишком тёмный;
- фигура почти растворяется в фоне;
- при просмотре как карточки Mora сцена теряет читаемость, хотя marker `0` виден.

**Почему важно:**
Шут — одна из главных карт входа в колоду и share-ready старший аркан. Если первая карта выглядит как тёмное пятно, доверие к качеству колоды падает сильнее, чем на редкой младшей карте.

**Ориентиры для будущей задачи:**
- делать отдельным visual-pass по `directives/04-add-tarot-cards.md`, без переписывания текстов в том же проходе;
- сохранить Rider-Waite-Smith канон: юная фигура на краю обрыва, котомка, белый цветок, маленький спутник, движение вперёд;
- усилить value separation: лицо/руки/цветок/силуэт должны читаться на фоне Mora UI;
- после визуала отдельно принять marker и только потом менять WebP.

### Medium · Проверить контраст карточного арта на фоне Mora UI

**Статус:** Open
**Дата:** 2026-06-19
**Где:** `public/assets/cards/*.webp`, dashboard/recent/journal/result UI
**Источник:** MVP-фидбек из чата: «слишком темно, картинки карт сливаются с фоном»

**Что видно:**
- пользовательский сигнал говорит не только про одну ошибку, а про риск всей текущей тёмной art direction;
- `docs/card-style.md` уже требует читаемый силуэт, но нужна проверка прямо в интерфейсе.

**Почему важно:**
Тёмный ритуальный стиль Mora работает только если главная фигура или символ считываются быстро. Если карты сливаются с фоном, мистичность превращается в визуальную усталость.

**Карты на визуальную актуализацию:**
- [ ] `fool` / «Шут» — слишком тёмный, фигура растворяется в фоне; подробности зафиксированы отдельным пунктом выше.
- [ ] `ten-of-pentacles` / «Десятка Пентаклей» — сделать изображение светлее и усилить контраст сцены с тёмным фоном Mora.
- [ ] `death` / «Смерть» — сделать белую пятилистную розу на чёрном знамени визуально узнаваемой как розу; сейчас символ соответствует канону RWS по форме, но в арте читается как абстрактный цветок и расходится с текстовым `visualHint`.

**Ориентиры для будущей задачи:**
- пройти QA-страницу и реальные блоки dashboard/journal;
- отметить проблемные карты списком, не перегенерируя всю колоду сразу;
- для новых карт проверять не только отдельный PNG/WebP, но и карточку на фоне Mora UI;
- начать с share-ready старших арканов и часто видимых карт.

### Low · Переименовать колонку «Тег» в дневнике

**Статус:** Open
**Дата:** 2026-06-19
**Где:** `/journal`, таблица дневника
**Источник:** MVP-фидбек из чата

**Что видно:**
- колонка `Тег` звучит технически и выбивается из голоса Mora;
- в этой колонке фактически показываются аркан/масть, а не пользовательский тег.

**Почему важно:**
Дневник должен ощущаться как история ритуалов, а не как админская таблица. Маленькие технические слова портят тон интерфейса.

**Ориентиры для будущей задачи:**
- решить label в рамках `Journal visual polish`;
- проверить варианты `Аркан`, `Тип`, `Масть`, `Слой` или другой короткий вариант прямо в таблице;
- не менять данные и механику reader.

### Medium · Почистить карточные тексты от AI-штампов

**Статус:** Open
**Дата:** 2026-06-19
**Где:** `public/assets/cards.js`, карточные `preview/full/share`
**Источник:** MVP-фидбек из чата по фразам и конструкциям

**Что видно:**
- пользователи считывают отдельные формулировки как AI-штампы;
- особенно раздражают `одного маленького действия`, `первым спокойным жестом к себе`, терапевтические абстракции и повторяющаяся конструкция `не X, а Y`;
- `docs/banned-phrases.md` уже пополнен конкретными примерами.

**Почему важно:**
Тексты — центральная поверхность Mora до появления AI-таролога. Если они звучат как общий AI-совет, продукт теряет голос и доверие.

**Ориентиры для будущей задачи:**
- при каждой актуализации старой карты проверять текст по `docs/banned-phrases.md`;
- отдельно искать повторяющуюся конструкцию `не ..., а ...`;
- не переписывать всю колоду одним проходом;
- начинать с карт, на которые пришёл явный фидбек, или с первых в QA-списке старого формата.

### Done · Подключить Custom SMTP после покупки домена

**Статус:** Done
**Дата:** 2026-06-17
**Где:** Supabase Auth email OTP, будущий sending domain, Resend/SMTP
**Источник:** проверка email-входа и лимитов встроенной почты Supabase

**Результат:**
- подключён Resend Free через отдельный `auth.moratarot.com`, корневая Beget-почта и verification TXT сохранены;
- Supabase Custom SMTP включён, OTP сокращён до 6 цифр, project quota поднята со встроенных 2 до 30 писем/час, resend interval сохранён 60 секунд;
- Site URL/redirects переведены на `moratarot.com`, письма оставлены OTP-only;
- production-письмо доставлено на подтверждённый Gmail автора. Проверку других почтовых сервисов проводить только на принадлежащих автору адресах.

### Done · `three-of-swords` добавлена последней картой колоды

**Статус:** Done
**Дата:** 2026-06-16
**Где:** `public/assets/cards/three-of-swords.webp`, карта `three-of-swords` / «Тройка Мечей»
**Источник:** несколько неудачных генераций во время проверки нового card-marker процесса

**Результат:** карта добавлена отдельным последним проходом после явных визуального и текстового аппрувов. Clean artwork сохраняет одно символическое сердце, ровно три цельных меча, дождь и тучи; встроенного marker нет. Карточная запись, ID, QA-сортировка и три `preview/full/share` сценария интегрированы, полная колода содержит 78 карт.

### Low · Старые карты нужно постепенно перевести на единый footer-marker

**Статус:** Open
**Дата:** 2026-06-14
**Где:** `public/assets/cards/*.webp`, нижний номер/символ карт
**Источник:** визуальная проверка последних добавленных карт и новый postprocess-flow для `eight-of-cups`

**Что видно:**
- у старых сгенерированных карт нижние markers отличаются по размеру, посадке, кернингу и декоративным элементам;
- `three-of-pentacles`, `seven-of-pentacles` и `four-of-swords` показывают проблему явно: `III` меньше, `VII` крупнее, у `IV` есть лишняя нижняя линия/ромб;
- с `eight-of-cups` введён новый процесс: карта генерируется без marker, marker накладывается helper-скриптом, проверяется через bottom comparison sheet и затем встраивается в фактуру.

**Почему не сейчас:**
- это не блокирует текущую механику карты дня;
- исправление всех старых карт требует отдельного визуального прохода и ручного аппрува, чтобы не испортить уже принятые ассеты;
- текущий приоритет — добавлять новые карты уже по улучшенному процессу.

**Ориентиры для будущей задачи:**
- пройти старые карты небольшими пачками;
- для каждой карты убрать/перегенерировать старый marker без поломки основного арта или аккуратно заменить нижнюю зону;
- наложить marker через `scripts/add-card-marker.py`;
- показывать bottom comparison sheet перед аппрувом;
- не менять ID карт, тексты и механику вытягивания.

### Low · Интерактивные 3D-карты Таро после основных функций

**Статус:** Open
**Дата:** 2026-07-31
**Когда брать:** после завершения основных функций сайта

**Идея:**
- оставить иллюстрации арканов 2D-текстурами и наносить их на одну универсальную 3D-модель карты;
- модель должна иметь лицевую сторону, рубашку, небольшую толщину, скругление, свет и тени;
- поддержать переворот, вытягивание из колоды, тасовку, веер, полёт, наведение, выбор и красивые переходы.
- рассматривать колоду не как декоративный эффект одного экрана, а как центральный бренд-объект Mora: пользователь приходит к одной узнаваемой колоде, которая сохраняет визуальную непрерывность между картой дня, раскладами и будущими сценариями;
- в браузере использовать одну универсальную геометрию карты и существующие 2D-иллюстрации как сменные лицевые текстуры; движения должны объяснять состояние продукта, а не быть красотой без функции.

**Предпочтительный простой путь:**
- сначала проверить Three.js или React Three Fiber и выбрать вариант, который лучше совпадает с актуальным стеком и уже установленными зависимостями;
- использовать одну модель карты и разные текстуры арканов;
- Blender подключать только при необходимости сложной геометрии, материалов, золотого тиснения, окружения или промо-рендеров;
- если Blender действительно нужен, Codex может управлять им через локальный MCP-сервер и Blender Python API (`bpy`);
- экспортировать модели в `GLB/GLTF` и оптимизировать размер, материалы и текстуры для браузера.

**Триггер будущего запуска:** `Итак, начинаем делать 3D-карты`.

После этой фразы нужно:
1. перечитать эту запись и актуальное состояние проекта;
2. проверить текущий стек, архитектуру и установленные зависимости;
3. выбрать самый простой подход без лишних инструментов;
4. составить полный пошаговый план;
5. объяснить, нужны ли Blender и MCP именно Mora;
6. если нужны — дать инструкции по установке Blender, созданию и подключению MCP, настройке `bpy`, экспорту и проверке;
7. реализовывать задачу поэтапно с визуальной проверкой, тестами производительности и адаптацией под мобильные устройства.

**Первый проверочный vertical slice:** одна универсальная модель карты → небольшая стопка → `rest` → `shuffle` → `fan` → `select` → `deal` → `flip` → `collect`. Blender отвечает за геометрию и материалы, браузерный WebGL — за интерактивное движение и состояния. Production и текущий Mora Next не менять до отдельного аппрува интеграции.

**Не делать заранее:** не устанавливать 3D-зависимости, Blender или MCP и не создавать модели до явного запуска этой задачи автором.

### Low · Перевёрнутые карты и мягкое обучение таро как будущая продуктовая фича

**Статус:** Open
**Дата:** 2026-06-09
**Где:** будущая механика карты дня, AI-таролог, onboarding / educational SEO surfaces
**Источник:** продуктовая ретроспектива во время расширения колоды

**Идея:**
- в будущем добавить поддержку перевёрнутых карт как отдельного положения карты при вытягивании;
- использовать это не только как “ещё одно значение”, но и как дополнительный слой случайности, вовлечения и повторного интереса;
- объяснять структуру таро через красивое погружение: onboarding, истории, карточные объяснения, возможно комиксные/визуальные форматы, а не сухую страницу “что такое таро”;
- использовать обучающие материалы как SEO-поверхность, но сохранять голос Mora и не превращать продукт в энциклопедию.

**Почему не сейчас:**
- текущая цель — собрать сильную прямую колоду и не усложнять MVP-механику;
- перевёрнутые карты потребуют `orientation`, новых текстов, snapshot-логики, UI-объяснения и аккуратного visual state;
- фича хорошо ложится на будущий AI-таролог, когда текст может учитывать не только карту, но и её положение, вопрос пользователя и контекст расклада.

**Что учесть заранее:**
- не писать текущие тексты так, будто прямое значение карты является единственно возможным навсегда;
- при будущей схеме данных заложить `orientation: upright | reversed`;
- для hardcoded режима понадобится отдельный набор reversed-readings или адаптер, который умеет объяснять перевёрнутую энергию карты простым языком.

### Medium · Dashboard card reader может микролагать при раскрытии карты

**Статус:** Open
**Дата:** 2026-06-09
**Где:** `/dashboard`, раскрытие карты дня и recent cards через `DashboardCardReader`
**Источник:** ручная проверка на production, субъективное ощущение микрофризов во время полёта карты

**Что видно:**
- при клике по карте анимация раскрытия иногда ощущается не идеально плавной;
- hover/tilt на миниатюрах работает гладко, проблема заметнее именно в shared-element полёте;
- эффект может быть сильнее при VPN или первом открытии карты, когда картинка ещё декодируется браузером.

**Почему может происходить:**
- во время полёта сейчас анимируются не только `transform`/`opacity`, но и `inset`, `width`, `height`, `border-radius` у внутренней картинки;
- overlay и отдельный `<img>` создаются в момент клика, что может дать decode/paint spike;
- одновременно гасится весь dashboard через `opacity`, а на нём есть `backdrop-filter`, тени и изображения;
- большая тень летящей карты может быть дорогой для compositor на некоторых устройствах/браузерах.

**Почему важно:**
Раскрытие карты — ритуальный момент Mora. Даже небольшой frame drop ощущается сильнее, чем в обычном UI, и мешает ощущению “дорогого” плавного продукта.

**Ориентиры для будущего фикса:**
- записать Performance trace в Chrome DevTools на production: раскрытие recent card и карты дня;
- проверить FPS meter / Paint flashing, отдельно с VPN и без VPN;
- убрать анимацию `width`/`height`/`inset`, оставить движение на `transform`/`opacity`;
- заранее декодировать картинку раскрываемой карты через `HTMLImageElement.decode()`;
- уменьшить/отложить тяжёлый `box-shadow` на время полёта;
- рассмотреть заранее смонтированный reader-layer, чтобы клик не создавал весь overlay с нуля.

### Critical · Dashboard не адаптирован под разные разрешения и мобильные устройства

**Статус:** Open  
**Дата:** 2026-06-07  
**Где:** `/dashboard`, responsive/devices, особенно mobile  
**Источник:** MVP-тесты и ручная проверка через DevTools responsive mode

**Что видно:**
- dashboard хорошо выглядит только на текущем Mac-разрешении пользователя;
- при изменении ширины экрана layout заметно разваливается;
- на mobile опыт критически плохой;
- местами невозможно нормально скроллить;
- блоки dashboard не имеют полноценной responsive-адаптации.

**Почему важно:**
Mora уже даётся людям на MVP-тест. До публичного релиза dashboard должен быть пригоден не только на одном Mac-разрешении, иначе ключевой авторизованный сценарий будет выглядеть сломанным для части пользователей.

**Когда брать:**
После того как основные desktop-механики dashboard будут доведены до стабильного состояния: card draw, expand/back, recent-card reader, journal interactions. Адаптив лучше делать отдельной большой задачей, но с маленькими визуальными инкрементами и проверкой на desktop/tablet/mobile.

**Ориентиры для будущей задачи:**
- сначала зафиксировать целевые breakpoint-диапазоны;
- проверить dashboard на 1440/1280 desktop, tablet и 375 mobile;
- отдельно решить mobile navigation и scroll behavior;
- не ломать текущую быструю production-анимацию;
- все изменения проверять глазами после каждого инкремента.
